# MyPOS — Контекст для AI-агента

> **Автоматически читается Claude Code в начале каждой сессии**
> **Последнее обновление:** 19 января 2026

---

## 1. ТЕКУЩИЙ СТАТУС

### Где мы сейчас
- **Продукт:** POS-система для HoReCa (кофейни, рестораны)
- **Первый клиент:** WeДrink (bubble tea, Казахстан)
- **Стадия:** MVP готов, Phase 0 (стабилизация + редизайн)

### Что работает
- POS-касса (веб, оптимизирована под iPad)
- Админка (товары, техкарты, ингредиенты, модификаторы)
- Offline режим (IndexedDB + Service Worker)
- WebSocket для Kitchen Display
- **Печать через RawBT** — Android + USB принтер, CP866, протестировано (см. секцию 8)

### Что НЕ работает / отложено
- USB печать на Windows (не удалось настроить, отложено)
- WiFi печать на XPrinter (настройки показывают "Unknown", отложено)

### Текущие задачи
1. **Провести dogfooding** — хотя бы 1 день на WeДrink (печать готова!)
2. **Редизайн UI** — современный интерфейс в стиле macOS (glass morphism)
3. ✅ **Проверить кириллицу** — CP866 работает правильно, протестировано на WeДrink

---

## 1.1 ROADMAP (ПЛАН РАЗВИТИЯ)

### Phase 0: Стабилизация ← МЫ ЗДЕСЬ
```
✅ Настроить печать (RawBT + USB вместо WiFi)
✅ Добавить печать бегунков (каждый напиток отдельно)
✅ Исправить кодировку CP866 для кириллицы
✅ Протестировать печать на WeДrink
□ Провести dogfooding на WeДrink (1+ день)
□ Собрать фидбек от кассира
□ Исправить критические баги
□ Редизайн UI (параллельно)
```
**Результат:** Работающий продукт, проверенный на практике

### Phase 1: SaaS Foundation
```
□ Multi-tenancy (organizations/workspaces)
□ Authentication (email + password, роли: owner, admin, cashier)
□ Subscription billing (выбрать провайдера для Казахстана)
□ Onboarding flow для новых клиентов
□ Лендинг для привлечения
```
**Результат:** Можно подключать клиентов и брать деньги

### Phase 2: Core Value
```
□ KitchenKit (веб-версия для телефона повара)
□ Улучшенная аналитика (PnL отчёты)
□ Интеграция с Kaspi QR
□ Cloud Print (альтернатива локальной печати)
```
**Результат:** Конкурентные преимущества перед Poster/iiko

### Phase 3: AI & Growth
```
□ AI рекомендации по закупу
□ Маркетплейс поставщиков
□ Курьерская служба
□ Мобильное приложение для клиентов
```
**Результат:** Экосистема

### Billing для Казахстана (исследовать)
- **Kaspi Pay** — удобно для клиентов, но проверить API для рекуррентных платежей
- **CloudPayments** — работает в СНГ, поддерживает подписки
- **Stripe** — международный, но может быть сложно с выводом в KZ
- **Paybox.kz** — локальный, поддерживает карты KZ банков

---

## 2. ВИДЕНИЕ ПРОДУКТА

### Большая цель
MyPOS — не просто касса, а **экосистема для HoReCa в Казахстане**:

```
┌─────────────────────────────────────────────────────────────┐
│                      MYPOS ECOSYSTEM                        │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  🏭 ПОСТАВЩИКИ          🏪 ТОЧКИ ПИТАНИЯ        👤 КЛИЕНТЫ │
│  ├─ Каталог товаров     ├─ POS Terminal         ├─ App     │
│  ├─ Заявки на закуп     ├─ KitchenKit           ├─ Заказы  │
│  ├─ Накладные           ├─ Inventory            └─ Loyalty │
│  └─ Аналитика           ├─ Analytics                       │
│                         └─ AI Recommendations               │
│                                                             │
│  🤖 AI LAYER                                                │
│  ├─ Рекомендации по закупу                                  │
│  ├─ Прогнозирование спроса                                  │
│  ├─ PnL отчёты                                              │
│  ├─ Risk Alerts                                             │
│  └─ Chat Assistant                                          │
│                                                             │
│  🚚 ДОСТАВКА                                                │
│  ├─ Курьерское приложение                                   │
│  ├─ Трекинг заказов                                         │
│  └─ Агрегатор (типа Glovo/Wolt)                            │
│                                                             │
│  💳 ИНТЕГРАЦИИ                                              │
│  ├─ Kaspi Pay                                               │
│  ├─ Halyk Bank                                              │
│  ├─ Freedom Pay                                             │
│  └─ ОФД (фискализация)                                      │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Планируемые модули

**Маркетплейс поставщиков:**
- Каталог товаров (поставщики сами заполняют)
- AI подсказывает названия ингредиентов
- Сравнение цен, поиск по городам
- B2B мессенджер внутри системы

**AI Control Center:**
- Чат с AI-ассистентом (знает всё о системе)
- Рекомендации по закупу (что, когда, сколько)
- Alerts (себестоимость выросла, маржа упала)
- PnL отчёты

**KitchenKit:**
- Приложение для поваров (смартфон)
- Статусы заказов, таймеры
- Отметки "готовится" / "готово"

**Курьерская служба:**
- Модуль доставки
- AI рассчитывает время
- Приложение для курьеров

### Монетизация
- Подписка для точек питания (тарифы)
- Подписка для поставщиков
- Комиссия с продаж (если разрешено)
- Реклама поставщиков в терминалах

---

## 3. DESIGN SYSTEM

### Стиль
macOS-like UI с glass morphism (как в дизайне от Stitch)

### Цветовая палитра

```css
/* Primary (Teal) */
--primary: #1f6b7a;
--primary-hover: #164d58;
--primary-light: #e0f2f4;

/* AI Accent (Purple) */
--ai-accent: #8b5cf6;
--ai-light: #ede9fe;

/* Backgrounds */
--background-light: #f5f5f7;  /* macOS-like */
--background-dark: #1e1e1e;
--surface: #ffffff;
--glass-bg: rgba(245, 245, 247, 0.75);

/* Text */
--text-primary: #1e293b;   /* slate-800 */
--text-secondary: #64748b; /* slate-500 */
--text-muted: #94a3b8;     /* slate-400 */
```

### Типографика
- **Шрифт:** Inter (уже используется)
- **H1:** 30px bold
- **H2:** 20px semibold
- **Body:** 14px normal
- **Small:** 12px medium

### Компоненты

**Сайдбар (Glass Panel):**
```css
width: 280px;
background: rgba(245, 245, 247, 0.75);
backdrop-filter: blur(24px);
border-right: 1px solid rgba(0, 0, 0, 0.05);
```

**Структура навигации:**
```
OPERATIONS     → Dashboard, POS Terminal, KitchenKit
CATALOG        → Товары, Техкарты, Ингредиенты
AI CONTROL     → Smart Assistant, Analytics (фиолетовый акцент)
SETTINGS       → Настройки
```

**Карточки:**
```css
background: #ffffff;
border-radius: 12px;
border: 1px solid rgba(0, 0, 0, 0.05);
box-shadow: 0 4px 20px -2px rgba(0, 0, 0, 0.05);
/* Hover: glow effect */
box-shadow: 0 0 15px rgba(31, 107, 122, 0.15);
```

### План редизайна

1. **Phase 1: Foundation** — layout, сайдбар, header
2. **Phase 2: Admin Pages** — таблицы, формы, карточки
3. **Phase 3: POS Page** — касса
4. **Phase 4: Polish** — dark mode, анимации, AI-элементы

---

## 4. ПРАВИЛА РАЗРАБОТКИ

### Tech Stack (НЕ МЕНЯТЬ без обсуждения)

**Backend:**
- Python 3.9+ / FastAPI
- SQLAlchemy 2.x / PostgreSQL
- Alembic для миграций
- Railway (auto-deploy из `main`)

**Frontend:**
- React 19 / Vite / TypeScript
- Tailwind CSS (без кастомных CSS файлов)
- lucide-react для иконок
- react-hot-toast для уведомлений

**Real-time & Offline:**
- Native WebSocket (НЕ socket.io)
- IndexedDB для offline очереди
- Service Worker + Background Sync

### ЗАПРЕЩЕНО

```
❌ Прямые изменения схемы БД — используй Alembic
❌ Удаление данных — используй soft delete (is_active = False)
❌ Class components — только functional + hooks
❌ axios — используй native fetch (api/client.ts)
❌ localStorage для критичных данных — используй IndexedDB
❌ console.log в продакшене
❌ Force push в main
❌ Новые фичи во время тестирования
```

### Обязательные паттерны

**Обновление PWA кэша:**
```javascript
// ВСЕГДА обновляй версию при изменении критичного кода!
// frontend/public/sw.js
const CACHE_NAME = 'mypos-vX'; // Увеличь номер версии
```
Когда обновлять:
- Изменения в печати (кодировки, форматы)
- Изменения в API клиенте
- Изменения в offline логике
- Любые критичные исправления

**Миграции БД:**
```bash
alembic revision --autogenerate -m "Description"
alembic upgrade head
```

**Error Handling (Frontend):**
```typescript
try {
  await api.createOrder(data);
  toast.success("Заказ создан");
} catch (error) {
  console.error("Error:", error);
  toast.error("Ошибка создания заказа");
}
```

**Offline-First:**
```typescript
// НИКОГДА не вызывай api.createOrder() напрямую
// ВСЕГДА используй offline queue
const { createOrder } = useOfflineQueue();
await createOrder(orderData);
```

---

## 5. ТЕХНИЧЕСКАЯ ИНФОРМАЦИЯ

### Продакшен URLs

| Сервис | URL |
|--------|-----|
| Backend | `https://backend-production-5eddb.up.railway.app` |
| Frontend | `https://frontend-production-e1a1.up.railway.app` |
| POS | `/pos` |
| Admin | `/admin` |
| Dashboard | `/dashboard` |
| API Docs | `/docs` |

### Структура проекта

```
MyPosSystem/
├── backend/
│   ├── app/
│   │   ├── models/      # SQLAlchemy модели
│   │   ├── routes/      # API endpoints
│   │   └── schemas/     # Pydantic схемы
│   ├── migrations/      # Alembic миграции
│   └── main.py          # FastAPI entry point
├── frontend/
│   ├── src/
│   │   ├── components/  # UI компоненты
│   │   │   ├── layout/  # AdminLayout, etc.
│   │   │   └── ui/      # Button, Input, Select
│   │   ├── pages/       # Страницы
│   │   │   ├── POSPage.jsx
│   │   │   ├── AdminPage.jsx
│   │   │   └── admin/   # Подстраницы админки
│   │   ├── utils/       # Печать, helpers
│   │   └── api/         # API клиент
│   └── public/
│       └── sw.js        # Service Worker
├── CLAUDE.md            # Этот файл
├── RULES.md             # Детальные правила
└── TESTING_GUIDE.md     # Гайд по тестированию
```

### Основные API endpoints

```
GET  /api/pos/items          # Товары для кассы
GET  /api/pos/categories     # Категории для кассы
POST /api/orders             # Создать заказ
GET  /api/orders/today       # Заказы за сегодня
GET  /api/orders/stats/today # Статистика
GET  /api/products           # Все товары (админка)
GET  /api/recipes            # Техкарты
GET  /api/ingredients        # Ингредиенты
```

### Команды разработки

```bash
# Backend (локально)
cd backend
source venv/bin/activate
uvicorn main:app --reload --host 0.0.0.0 --port 8000

# Frontend (локально)
cd frontend
npm run dev

# Деплой
git push origin main  # Railway автоматически задеплоит
```

---

## 6. ДЛЯ AI-АГЕНТА

### При старте сессии
1. Прочитай этот файл (автоматически)
2. Спроси пользователя что нужно сделать
3. Используй TodoWrite для планирования задач

### Частые запросы и как реагировать

**"Печать не работает":**
- Сначала проверь что используется CP866 (не UTF-8)
- Проверь что PWA обновилась (CACHE_NAME в sw.js)
- Попроси пользователя очистить кэш браузера или переустановить PWA
- USB печать на Windows отложена, не тратить время

**"Сделай редизайн X":**
- Используй цвета и стили из секции Design System
- Применяй glass morphism для сайдбаров
- Teal (#1f6b7a) — основной цвет
- Purple (#8b5cf6) — для AI-элементов

**"Добавь новую фичу":**
- Проверь что это не противоречит RULES.md
- Предложи план ПЕРЕД кодингом
- Используй существующие паттерны

**"Исправь баг":**
- Сначала воспроизведи
- Проверь Railway логи
- Не ломай существующий функционал

### Контакты владельца
- **Telegram:** @muahhidun
- **WhatsApp:** +77071272789
- **Проект:** WeДrink (Казахстан)

### Стиль коммуникации
- Пиши кратко и по делу
- Коммиты на русском
- UI тексты на русском
- Код и комментарии — английский или русский

---

## 7. ИЗВЕСТНЫЕ ПРОБЛЕМЫ

### CategoryType enum
```
Ошибка: invalid input value for enum categorytype: "product"
Решение: POST /api/admin/fix-categorytype-enum
```

### WebSocket disconnects
Ping/pong keep-alive уже реализован в sw.js

### Offline orders не синхронизируются
1. Проверь DevTools → Application → IndexedDB
2. Проверь Service Worker registration
3. Кликни "X в очереди" для ручной синхронизации

---

## 8. ПЕЧАТЬ ЧЕРЕЗ RAWBT (ANDROID + USB)

### Оборудование
- **Планшет:** Android (любой)
- **Принтер:** XPrinter A160M (80mm, USB)
- **Приложение:** RawBT (из Google Play)

### Как работает
```
MyPOS (PWA) → rawbt:base64,<data> → RawBT App → USB → Принтер
```

### Настройка
1. Установить RawBT из Google Play
2. Подключить принтер по USB к планшету
3. В RawBT: выбрать принтер USB
4. **ВАЖНО:** Установить MyPOS как PWA (Add to Home Screen)
5. В настройках MyPOS: IP принтера = `rawbt`

### Файлы печати
```
frontend/src/utils/printerESCPOS.js  — базовый класс ESC/POS
frontend/src/utils/receiptPrinter.js — чеки и бегунки
frontend/src/utils/labelPrinter.js   — этикетки (для другого принтера)
```

### Кодировка
- **CP866** для всех методов печати (принтер XPrinter не поддерживает UTF-8)
- Команды: `INTL_CHARSET_RUSSIA` + `CHARSET_CP866`

Логика в `printerESCPOS.js`:
```javascript
textToBytes(text) {
  return this.textToBytesCP866(text);  // CP866 для всех
}
```

### Что печатается
1. **Чек** — полный чек с итогом (printReceipt)
2. **Бегунки** — каждый напиток отдельно с обрезкой (printRunners)
3. **Чек + Бегунки** — всё вместе в одном вызове (printReceiptWithRunners)

**ВАЖНО:** Для RawBT используй `printReceiptWithRunners` — иначе напечатается только последний вызов!

Пример бегунка:
```
      #123
  ----------------
   Матча Латте
     Большой

      1 / 2
      14:35

[ОБРЕЗКА]
```

### Если кириллица не работает
1. Проверить что используется CP866 (не UTF-8)
2. Проверить команды `INTL_CHARSET_RUSSIA` + `CHARSET_CP866` в начале печати
3. Убедиться что сайт установлен как PWA
4. Проверить что RawBT обновлён до последней версии

### Если печатаются только бегунки (без чека)
- Использовать `printReceiptWithRunners()` вместо двух отдельных вызовов
- RawBT обрабатывает только один `window.location.href` за раз

### Кэширование PWA и обновления

**Проблема:** PWA агрессивно кэширует код через Service Worker. После обновления кода на продакшене, устройства могут продолжать использовать старую версию.

**Признаки проблемы:**
- Код обновлён на сервере, но изменения не видны на планшете
- Печать работает неправильно после исправления багов
- Разное поведение между устройствами

**Решение 1: Обновить версию кэша (для разработчика)**
```javascript
// frontend/public/sw.js
const CACHE_NAME = 'mypos-v4'; // Увеличить версию
```
После мерджа в `main` и деплоя, PWA обновится автоматически при следующем запуске.

**Решение 2: Принудительное обновление (для пользователя)**

Вариант А — Очистка кэша:
1. Chrome → Меню (⋮) → История
2. Очистить данные → Изображения и файлы в кэше
3. Перезагрузить страницу (свайп вниз)

Вариант Б — Переустановка PWA:
1. Удалить приложение с главного экрана
2. Открыть сайт в Chrome
3. Установить заново (Add to Home Screen)

**ВАЖНО:** При изменении логики печати, кодировок или критичных функций — **ВСЕГДА обновляй CACHE_NAME** в sw.js!

---

*Этот файл — единственный источник правды для AI-агента. Обновляй его при важных изменениях.*
